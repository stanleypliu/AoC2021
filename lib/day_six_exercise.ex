defmodule DaySixExercise do
  import Helpers

  def calculate_ages(filename) do
    days = 80

    list_of_timers_at_start = read_file(filename)
                      |> String.split(",", trim: true)
  end

  def
end
